{
	"name": "worker-libreflare-template",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"libreflare:config": "node scripts/generate-wrangler-config.mts",
		"deploy": "npm run libreflare:config && wrangler deploy",
		"dev": "npm run libreflare:config && wrangler dev",
		"start": "npm run dev",
		"typecheck": "tsc -b",
		"lint": "eslint src/**/*.ts",
		"cf-typegen": "npm run libreflare:config && wrangler types"
	},
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@types/node": "^24.12.3",
		"eslint": "^10.2.0",
		"jsonc-parser": "^3.3.1",
		"typescript": "^6.0.3",
		"typescript-eslint": "^8.58.1",
		"wrangler": "^4.54.0",
		"yaml": "^2.8.4"
	},
	"dependencies": {
		"@libreflare/worker-runtime": "^0.2.2"
	}
}
